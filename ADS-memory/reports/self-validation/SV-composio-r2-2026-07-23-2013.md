# Self-validation: `@jini/composio` R2 remediation

- Timestamp: 2026-07-23 20:13 America/Los_Angeles
- Outcome: PARTIAL
- Contract:
  `ADS-memory/.local-artifacts/external-audit/remediation/20260724-composio-r2-remediation-contract.md`
- Contract SHA-256:
  `cd8e97c63d2e38352fc66822a24fd4589a1d61618a57df28c796a33ac4bca562`

## Why the outcome is PARTIAL

Jini's host runtime-validation declaration remains a draft with no boot
command, healthy signal, critical path, negative path, or teardown command.
`@jini/composio` is also a headless package and no live Composio API key or
OAuth credential is available. The injected provider/service harness was run,
but a hosted OAuth and execution round trip could not be performed.

The package test gate additionally needs TDD recertification: 12 legacy tests
assert behavior that the locked R2 contract now forbids. This is not treated as
a runtime PASS or as permission to weaken the fail-closed implementation.

## Environment preflight

- Package dependencies installed: yes
- Package typecheck available: yes
- Injected provider/service contract harness available: yes
- Live Composio credentials: no
- Declared host boot/health/teardown harness: no; declaration is `DRAFT`

## Checks run

| Check | Result | Evidence |
|---|---|---|
| R2 RED-to-GREEN comparison | PASS | All 30 tests that failed in the certified RED JSON now pass; 0 unresolved |
| Focused R2 suite | PARTIAL | 105/117 pass; the 12 failures are legacy expectation conflicts listed below |
| Full package suite | PARTIAL | 109/121 pass; same 12 conflicts |
| Package typecheck | PASS | `pnpm --filter @jini/composio typecheck` |
| Package build | PASS | `pnpm --filter @jini/composio build` |
| Repository guard | PASS | self-test passed; zero package-boundary violations |
| Coverage gate | NOT RUN | Full suite must be recertified first; no ignores or artificial calls were added |

## Critical-path verification

The injected contract harness verified the security-critical path:

- execute input and identifiers are rejected before provider I/O when malformed
  or oversized;
- execution performs strict current per-connector hydration;
- current tool-list failure denies execution;
- a successful current tool list that omits a curated static tool revokes that
  tool;
- matching credential evidence is required for connected status and execution;
- current parameter-map schemas are decoded and validated fail-closed; and
- provider output is structurally bounded and redacted.

All certified R2 tests for that path are GREEN.

## Negative-path verification

The injected harness verified that aggregate catalog hydration is rejected
before provider requests, provider-discovered uncurated tools remain
display-only, `ACTIVE` OAuth without account identity is rejected, wrong-user
or wrong-connector credentials expose no account label, secret symlinks are
rejected, and lock release preserves other owners.

## Remaining legacy-test conflicts

Eleven service tests use a harness whose successful `/tools` response defaults
to an empty list while still expecting static tools to execute. R2 contract
item 2 requires a successful current omission to revoke that authority.

One provider test expects `listDefinitions({ hydrateTools: true })` to return a
fallback catalog. R2 contract item 6 requires aggregate hydration to be
rejected before provider I/O.

One of the eleven service tests also expects an in-memory `connected` status
without persisted credential evidence to be sufficient. R2 contract items 7
and 10 require the service to project `available` and deny execution.

## Required next validation

1. TDD Agent recertifies the 12 stale expectations without changing the locked
   R2 outcomes.
2. TestRunner reruns the full test and 100% coverage gates.
3. A credentialed environment performs a hosted OAuth/account-ownership/tool
   execution round trip.

