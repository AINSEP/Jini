/**
 * DevOps domain marker. Import a concrete capability from an explicit
 * subpath, such as `@jini-ai/devops/deploy`.
 *
 * Planned sibling capabilities: `./source-control` and `./ci-cd`.
 */
export {};
