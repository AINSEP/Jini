/**
 * @jini/agentic — placeholder barrel, populated once the nine universal files land (step 2 of
 * the 2026-07-26 extraction). Left intentionally minimal so the two-tsconfig DOM split can be
 * verified before any real content moves.
 */
export const AGENTIC_PACKAGE_PLACEHOLDER = true;
